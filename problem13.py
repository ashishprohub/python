# Replace the double space from problem 12 with single space 
name = "Ash   i  sh  "
print(name.replace("  "," "))
print(name) # Strings are immutable which means that you cannot change these by running functions on them.